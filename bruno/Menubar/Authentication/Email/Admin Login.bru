meta {
  name: Admin Login
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/auth/admin/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "john.doe@example.com",
    "password": "Password1@"
  }
}
